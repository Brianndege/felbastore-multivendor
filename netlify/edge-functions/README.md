# Netlify Edge Functions

This directory is enabled via `netlify.toml` under `[edge_functions]`.

Use this folder when you need ultra-low-latency request processing at the edge.
For this project, core API/server logic is handled by Next.js API routes and Netlify Functions.
